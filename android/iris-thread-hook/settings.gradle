pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // Xposed API (compileOnly). If this host is down, the api-82 jar is also on jitpack.
        maven { url = uri("https://api.xposed.info/") }
    }
}
rootProject.name = "iris-thread-hook"
